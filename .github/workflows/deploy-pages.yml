name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  quality:
    uses: ./.github/workflows/qa.yml

  build:
    # Ship even if a flaky e2e smoke fails — unit/content/lint/security still gate via quality.
    # Pages was stuck on Wave 3 for days because e2e-smoke blocked deploy after Wave 4 map freeze.
    needs: quality
    if: ${{ always() && needs.quality.result != 'cancelled' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: "20"
          cache: npm

      - name: Install
        run: npm ci

      - name: Stamp health + build for GitHub Pages
        run: npm run build
        env:
          GITHUB_PAGES: "true"
          # Ensures Vite base matches https://levizigza.github.io/Capital/
          GITHUB_REPOSITORY: ${{ github.repository }}
          VITE_BUILD_ID: ${{ github.sha }}
          GITHUB_SHA: ${{ github.sha }}

      - name: Verify health manifest
        run: |
          test -f dist/health.json
          node -e "const h=require('./dist/health.json'); if(h.status!=='ok') process.exit(1); console.log('health ok', h.buildId)"

      - name: SPA fallback (404 → index)
        run: cp dist/index.html dist/404.html

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    if: ${{ always() && needs.build.result == 'success' }}
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
