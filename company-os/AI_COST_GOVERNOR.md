# AI Cost Governor (pointer)

Per-workflow budgets (model, tokens, steps, tools, retries, runtime, $ cost, fallback).
Route by complexity. **Evals required before model downgrade.**
Flag when AI cost grows faster than economic value.

Code: `src/business/aiCostGovernor/` · Docs: `docs/AI_COST_GOVERNOR.md`
