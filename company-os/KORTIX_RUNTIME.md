# Kortix runtime (pointer)

**Proposal only.** Capital must not be rewritten around Kortix.

See `docs/KORTIX_RUNTIME_EVAL.md` for the architecture evaluation:

- Vendor-neutral Company OS / schemas / memory / economics / approvals stay in Capital  
- Kortix (if used) is an **optional execution runtime** behind `ExecutionRuntimePort`  
- Least-privilege secrets/connectors per workflow  
- **Adapter not implemented until the proposal is accepted**
