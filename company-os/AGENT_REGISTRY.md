# Agent Registry (pointer)

Formal schema for **potential** Capital agents. Default roster is empty.

Do **not** create agents to fill capability roles. Instantiate only when a measurable
workflow needs independent context/tools/reasoning.

Code: `src/business/agentRegistry/` · Docs: `docs/AGENT_REGISTRY.md`
