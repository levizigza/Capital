# Institutional memory (pointer)

Capital’s **product** is the Harbor game. This folder’s truth hierarchy (FACT → canon → …)
is enforced in code by:

→ `src/business/institutionalMemory/`

Eight storage classes, promotion ladder, version/rollback, and the rule that **AI output
never auto-promotes to canonical**. See `docs/INSTITUTIONAL_MEMORY.md`.
