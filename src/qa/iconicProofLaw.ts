/**
 * Iconic 100% proof law — closes the three remaining asterisks.
 *
 * 1. `__QA__` is the official headless harness (not an apology).
 * 2. 100% scores Harbor · Cove → Paycheck → Credit only; later/side shores are freeze-parked.
 * 3. Proof lives on the craft branch + live boards; merge is the ship step, not a craft gap.
 */

export const ICONIC_PROOF_SCOPE =
  "Harbor · Cove → Paycheck → Credit" as const;

export const ICONIC_PROOF_NOT_SCORED = [
  "era SIDE SHORE chapters (outer ring extras)",
  "docs/iconic-later.md parked islands + digression minigames",
  "Nathan Project / BMO / CBE",
  "fake multiplayer / Family Room backend",
] as const;

/** Official cold bank — scripts/ + e2e that earn 100%. */
export const ICONIC_PROOF_COLD_SCRIPTS = [
  "scripts/cold-ashore-cove-retell.mjs",
  "scripts/cold-spine-retell.mjs",
  "scripts/cold-human-triangle-pass.mjs",
  "scripts/cold-full-cove-chain.mjs",
  "scripts/cold-full-paycheck-chain.mjs",
  "scripts/cold-full-credit-chain.mjs",
  "scripts/cold-fail-mute-seal.mjs",
  "scripts/cold-day2-coin-echo.mjs",
  "scripts/cold-structure-soft-beat-exit.mjs",
] as const;

/**
 * Harness may open systems (headless WebGL walk flakes).
 * These surfaces must still be live UI — not seeded text.
 */
export const ICONIC_PROOF_HARNESS_MAY = [
  "enterIsland / openHub / openTravel",
  "talkNpc / collectItem / startMinigame",
  "seedSignatureLoop / prepareDay2Echo",
  "enterMoneyStructure / enterStructureSoftBeat",
  "completeMinigame for fail-dignity cold",
  "harbor-skip-3d when Canvas veil stalls",
] as const;

export const ICONIC_PROOF_MUST_BE_LIVE = [
  "Talk Battle choices + Leave / Esc",
  "Coin Sort play + mastery quiz",
  "Soft Beat overlay leave + money-structure-exit",
  "fail overlay organ verb (Coin holds)",
  "mute cinema kid sentence",
  "Seal chase chrome absent unless isSealChasing",
  "Harbor / shore host remount-free after structure exit",
  "organ kid sentences: Coin holds · Clock shelters · Spiral withstands · Memory keeps",
] as const;

export const ICONIC_PROOF_BOARDS = [
  "docs/full-game-craft-board.md",
  "docs/tutorial-craft-board.md",
  "docs/iconic-later.md",
] as const;

export const ICONIC_PROOF_BAR_HOLDS_NEEDLE = "MVP iconic bar HOLDS";
