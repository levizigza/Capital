/**
 * Kenney Nature Kit (CC0) OBJ subset lives under public/assets/3d/kenney/.
 * Default OFF so a failed MTL/OBJ fetch cannot leave Canvas Suspense blank.
 * Procedural props always render; set true once assets are verified locally.
 */
export const KENNEY_ENABLED = false;
